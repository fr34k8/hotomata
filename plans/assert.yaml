# assert
#
# Assertion helper to abort execution on certain condition
---

vars:
  that:

plans:
  - $name: Run assertion
    $run: |
      {% if that %}
      echo '{"action":"continue","status":"success"}'
      {% else %}
      echo '{"action":"abort","status":"warning"}'
      {% endif %}
    $local: true
