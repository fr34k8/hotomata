---

- machines:
    name: "lb*"
    groups_include: ["lb"]
  vars:
    nginx_version: "1.4.2"
  plans:
    - common
    - merd-agent
    - nginx

- machines:
    name: "web*"
    groups_include: ["web"]
  vars:
    node_version: "0.12.0"
  plans:
    - common
    - merd-agent
    - nodejs
